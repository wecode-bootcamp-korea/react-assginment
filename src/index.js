import React from "react";
import ReactDOM from "react-dom/client";
import Router from './Router' //라우터연결 방법


const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<Router/>);


//render 안 변수를 바꾸기 위해 라우터를 사용 